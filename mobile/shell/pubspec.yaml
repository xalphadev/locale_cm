name: shell
description: "Shared WebView app shell for the Locale consumer & merchant iOS apps."
version: 0.0.1
publish_to: "none"

environment:
  sdk: ^3.12.2
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.13.0
  webview_flutter_wkwebview: ^3.22.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
